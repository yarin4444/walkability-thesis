import React from 'react';
import ReactDOM from 'react-dom/client';
import WalkabilityFlow from './WalkabilityFlow';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WalkabilityFlow />
  </React.StrictMode>
);